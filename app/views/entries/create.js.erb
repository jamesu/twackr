<% @simple_entry = 1 %>
<%= js_prepend('#entries', haml(:'entries/_entry', :locals => {:entry => @entry} ) ) %>;
<% unless @entry.terminated? %><%= js_call("Timer.register", @entry.id) %>;<% end %>
<%= js_element_call("entry_#{@entry.id}", 'slideDown') %>;
